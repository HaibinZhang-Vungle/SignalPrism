# Stub — floor optimization DCN v2 uses all 23 features unconditionally.
# Feature engineering is inline in the train notebook (no per-feature processor map needed).
FEATURE_TO_PROCESSORS = {}

def derive_feature_engineering(feature_selected):
    return []
